import {useEffect,useState} from 'react'; import api from '../services/api'; import {Trophy} from 'lucide-react'; export default function Leaderboard(){const [rows,setRows]=useState([]),[error,setError]=useState(''),[live,setLive]=useState(true);useEffect(()=>{let on=true;const load=()=>{api.get('/leaderboard').then(r=>{if(on)setRows(r.data.leaderboard||[])}).catch(()=>{if(on)setError('Could not load leaderboard. Check the backend connection.')})};load();const t=setInterval(()=>{if(document.visibilityState==='visible')load()},15000);return()=>{on=false;clearInterval(t)}},[]);return <div><span className="eyebrow">GLOBAL RANKINGS</span>{live&&<span className="live-dot" onClick={()=>setLive(false)} title="Auto-refreshes every 15s">● LIVE</span>}<h1 className="page-title">Leaderboard</h1>{error&&<div className="error">{error}</div>}<div className="leader">{rows.map((u,i)=><div className={'rank'+(i<3?' rank-'+(i+1):'')} key={u._id}><strong>#{i+1}</strong><Trophy/><div><b>{u.name}</b><span>{u.gamesPlayed} missions • {(u.badges||[]).join(' • ')||'Rookie'}</span></div><em>{u.totalScore}</em></div>)}{!rows.length&&<p className="muted">No players yet.</p>}</div></div>}
